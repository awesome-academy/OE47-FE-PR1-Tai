mixin navigate(index)
	section.navigate
	.container.navigate__contain
		.row
			ul.navigate__link
				each n,i in index
					li
						a(href=n.url)= n.name
							if i != index.length-1
								i.fas.fa-chevron-right
